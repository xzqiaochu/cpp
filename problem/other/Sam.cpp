#include <bits/stdc++.h>

//#define cin fin
//#define cout fout

using namespace std;

ifstream fin("sam.in");
ofstream fout("sam.out");
using namespace std;

int main() {
	int n, k, p;
	cin >> n >> k >> p;
	int * a;
	a = new int[p];
	for (int i = 0; i < p; i++) cin >> a[i];
	int cnt = 0;
	for (int i = 0; i < p; i++) {
		
	}
}
